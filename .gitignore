__pycache__/
*.pyc

# scratch/debug outputs from ad-hoc testing sessions, not real deliverables
outputs/sahi_test/
outputs/waldo_test/
outputs/tracks/*.mp4

# regenerable TensorRT export artifacts (see export_trt.py) - large, rebuild locally instead
weights/*.onnx
weights/*.engine

# Hailo export artifacts (export_hailo.py): regenerable, and the .hef is compiled on the x86 PC
calib/
models/*.onnx
models/*.hef
