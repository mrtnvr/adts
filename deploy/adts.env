# Arguments for the adts service (/etc/default/adts). Edit, then: sudo systemctl restart adts
# All options: python3 -m adts --help
ADTS_ARGS="--source picam --model models/waldo_yolov8m_640.hef --hfov 66 --mav /dev/ttyAMA0 --mav-baud 921600 --sysid 1 --hdmi --record /home/pi/rec"
